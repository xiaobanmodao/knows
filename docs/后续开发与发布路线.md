# 知识通后续开发与发布路线

> 更新日期：2026-08-10
>
> 当前路线分支：`codex/roadmap-v1.11`
>
> 当前开发版本：`1.10.1-dev.1`

## 1. 产品定位

知识通定位为面向初中学生的数学、英语、物理、化学和生物知识查阅工具。核心价值是“快速找到、容易看懂、随时记下”，不做在线测评、打分、学完任务、单元输出任务或家长管理。

后续开发坚持四个原则：

1. 先保证内容准确、完整和可检索，再增加新能力。
2. 教材目录用于核对范围，讲解、例句、例题和插图保持原创。
3. 文字内容在图片或云服务失败时仍可完整阅读。
4. 稳定 ID 、旧链接、收藏、最近浏览和本地笔记必须向前兼容。

## 2. 当前基线

### 2.1 内容规模

| 学科 | 当前内容 |
|---|---|
| 数学 | 29 章、29 专题、89 个详细教材小节、89 条独立推导、36 个方法模板、445 道原创示例 |
| 英语 | 5 册、42 个教材单元、336 个详细词条、84 个详细单元语法点、924 个例句；已核对五册全部补深 |
| 物理 | 3 册、22 章、84 个知识点、22 个方法模板、252 道原创示例、29 个实验 |
| 化学 | 九年级 10 个专题，按专题、知识点、实验和方法组织的查阅内容 |
| 生物 | 6 个单元、36 个知识点、6 个方法模板、108 个原创例子和 6 个观察说明 |

### 2.2 已具备能力

- 三科教材目录与专题索引分开展示。
- 统一搜索学科、教材单元/章节、专题、知识点和方法模板。
- 知识页支持分层阅读、关键图解、上下篇、复制和云图片失败降级。
- 收藏、最近浏览、继续阅读、阅读位置、本地笔记和自定义标签。
- 716 张运行时云图片具备尺寸、字节数、哈希和重复检查。
- 微信开发者工具 iPhone 与 Nexus 5 模拟器核心路由通过，当次自动化无项目错误或异常。

## 3. 当前发布收尾：v1.10.1

当前版本仍是 `1.10.1-dev.1`，发布分支只处理发布证据和阻塞性缺陷，不混入 v1.11 内容。

1. 在微信开发者工具确认登录账号拥有项目 AppID 的开发权限，并重新打开项目建立绑定。
2. 在路线工作树执行 `node scripts/check-release-tool-state.js --project /Users/hht/Desktop/knows/.worktrees/release-regression-v1.10.1`，先确认配置、登录和最近一次上传日志的真实状态。
3. 运行 `node scripts/run-release-preview.js`，生成当前构建的二维码、完整日志和包体报告。
4. 在 iPhone 与 Android 实体设备完成回归清单 A1-A15，保存本地证据文件和截图。
5. 执行 `node scripts/check-release-readiness.js --require-device-evidence` 及全量内容检查。
6. 只有实体设备证据、包体报告和严格门禁全部通过后，才更新为 `1.10.1-rc.1`、生成体验版并继续审核流程。

当前外部阻塞是微信服务端返回 `41002 appid missing`，因此包体报告和实体设备证据尚未具备，不能把模拟器结果当作真机发布证据。详见 [v1.10 发布前实体设备回归清单](./v1.10发布前实体设备回归清单.md)。

## 4. 已完成：v1.4.0 分包与内容底座

这部分已经完成，作为当前发布版本的稳定底座。后续只允许修复回归问题，不再重复建设同一套分包能力。

### 4.1 学科分包

- 主包只保留首页、搜索索引、收藏、我的和通用组件。
- 数学、英语、物理分别进入独立分包，学科内目录、知识数据和页面同包放置。
- 搜索主包只保留轻量索引：ID、标题、别名、类型、学科和目标路由。
- 收藏、最近浏览和笔记继续使用稳定 ID，跳转时再加载相应分包。
- 验收目标：主包小于 `700 KiB`，单科分包小于 `1MB`，首页不同步加载三科全量正文。

v1.4 初始分包已完成，基线预览包体为：主包 `495.5 KiB`、英语 `268.3 KiB`、数学 `485.9 KiB`、物理 `212.8 KiB`，总计约 `1.4 MiB`。推进到 v1.5.1 物理补深后，最新实测为主包 `583.5 KiB`、英语 `519.0 KiB`、数学 `485.9 KiB`、物理 `242.1 KiB`，总计约 `1830 KiB`；仍未配置自动预下载，主包与单科分包均在目标范围内。

### 4.2 统一内容规格

- 为章节、单元、知识点、单词、语法、公式、实验和方法建立可机器校验的 schema。
- 补齐 `reviewedAt`、`sourceRefs`、`status`、`aliases` 和稳定父级关系。
- 将 `utils/math.js` 中的大型内容组装逻辑按查询、搜索索引和数据构建拆分，保留对旧 API 的兼容层。
- 内容校验输出“新增、修改、删除、引用失效”差异报告，避免目录调整时静默破坏链接。

### 4.3 兼容与回归

- 存储结构版本升级前先建立收藏、浏览位置、笔记和标签迁移用例。
- 深链接回归覆盖旧数学链接、英语单词/语法卡片定位和物理章节链接。
- 分包加载失败时给出明确重试入口，不展示空白页。

当前 8 个旧主包路径均已改为轻量跳转页，833 条主包搜索索引具备内容源哈希；v1.3 内容差异报告为新增 0、修改 0、删除 0。Nexus 5 与 iPhone 14 Pro Max 模拟器已完成三科目录和代表性内容页回归。

## 5. 已完成：v1.5.0 三科内容补深

### 5.1 英语

- 已核对五册全部完成：42 个单元、336 个补深词条、84 个补深语法点，英语例句总数由 504 增至 924。
- 336 个核心词逐词补充音标、可数性/及物性、词形变化、高频搭配、语境例句和中文辨析。
- 84 个语法点补齐结构式、使用条件、时间线或句子成分图、肯定/否定/疑问变式和易混对比。
- 单元页支持“全部、表达、单词、语法”快速筛选，单词摘要折叠，并保留搜索直达卡片。
- 不复制教材原文、课文或听力材料。

### 5.2 物理

- 已完成 84 个知识点的结构化补深，共关联 225 组物理量、32 组方向规则，并逐项补齐符号、单位、公式条件和适用边界。
- 已完成 29 个实验的统一结构，在目的、器材、操作、现象、结论、误差和安全基础上补充方法、控制变量、数据记录和稳定实验 ID。
- 绘图专项复核光路、受力箭头、电流方向、电路连接点、坐标和单位。

### 5.3 数学

- 已按 89 个教材小节逐项补齐定义或定理的成立条件、公式变形和图形关系。
- 已为 89 个小节分别增加独立推导和“为什么成立”，避免只给结论或使用通用套话。
- 已建立 12 类数学与物理、测量、统计和工程情境的人工复核联系，搜索可直达推导关键词。

### 5.4 内容验收

- 每次只合并一册或一个明确专题，不做跨三科的大批量无差别改写。
- 内容变更须同时通过结构、引用、搜索、图片和复核元数据检查。
- 抽查页面不出现通用套话、空字段占位、错误公式、语法不对应或图文无关。

## 6. 已完成：v1.6.0 查阅效率与本地知识簿

- 已完成统一知识索引第一阶段：数学/物理 173 条公式、英语 336 个单词与 84 个语法点、物理 29 个实验，总计 622 个直达入口，不新增内容导航层级。
- 单词和语法复用现有搜索索引，仅生成公式与实验摘要，保持主包轻量；实验可定位到知识页内对应模块。
- 已完成搜索体验第二阶段：标题、摘要与标签命中高亮，双向别名管理，以及全半角、上下标、乘除号和空格的公式符号归一化。
- 后续继续扩充英语词形还原规则，并基于真实搜索场景复核别名；本地搜索记录不上传。
- 已完成本地笔记第一阶段：按学科、标签和标题/正文关键词筛选，支持组合条件、计数、重置和命中高亮。
- 已完成本地数据备份与恢复：覆盖收藏、最近浏览、搜索记录、阅读位置、笔记与数学年级偏好，支持校验预览、合并和覆盖恢复。
- 备份文件仅由用户主动转发或选择，不要求登录，不上传笔记内容；覆盖恢复须二次确认，写入失败自动回滚。
- 增加“相关知识”的人工审核关系，优先补齐数学与物理的比例、图像、几何和单位换算联系。
- 增加字号、行距和图片宽度的阅读设置，仅在本地保存。

## 7. v1.11：内容来源维护与生产工具

- 将 JS 数据的手工维护逐步迁移为结构化 JSON/CSV 导入，运行时仍生成小程序可直接加载的静态模块；当前已完成元数据输入契约和 JSON/CSV 规范化构建入口第一阶段，详细批次见 [v1.11 后续开发路线](./v1.11后续开发路线.md)。
- 已完成全学科内容批次首轮干跑：23 个学科/类型范围覆盖当前 948 个实体，逐批校验实体数量、示例/实验/资源统计、复核状态、范围隔离和零差异；可用 `node scripts/check-content-source-batches.js --report dist/content-audit/content-source-batches.json` 留存确定性报告，旧别名由内容源目录单独校验。
- 已登记英语/物理 8 个、化学/生物 11 个高风险批次，分别生成 `subject-high-risk-batches.json` 和 `chemistry-biology-high-risk-batches.json`；批次门禁覆盖词法/语法补深、物理公式与实验、化学方程式与实验、生物观察与复核元数据，不改变运行时内容源。
- 公式、单位、方向、实验、化学方程式和生物观察字段继续由各学科专项准确性检查覆盖；批次检查只用于审计，不改变运行时内容源。
- 已完成元数据级内容源目录：生成 `dist/content-audit/content-source-catalog.json`，覆盖 948 个实体和 89 条数学旧别名，记录稳定键、父级、复核状态、来源键、内容哈希和资源数量，不复制正文；发布准备会重建并校验该产物，并支持基线差异审计。
- 提供稳定 ID 生成、引用检查、搜索索引生成、资源清单和内容差异报告；内容源批次 manifest 及每个批次额外记录 `sourceKind`，当前源夹具为 `current-fixture`，严格外部证据门禁会逐批要求 `external-source`。
- 已补齐五科复核元数据中的稳定来源键；严格内容审计拒绝无 key 的 URL 来源和同一 key 对应多个 URL，避免来源页面替换后无法追踪。
- 生成图片后自动执行尺寸、文件大小、哈希重复、引用完整性和云路径检查。
- 每条内容保留来源、复核人/方式、复核日期和变更摘要，支持待复核、已复核、已发布状态。

## 8. 暂不进入主线的能力

以下能力不进入 `v1.4-v1.7` 主线：

- 在线答题、自动评分、学完自测和单元任务。
- 错题本、掌握状态、排名、打卡和家长端。
- AI 自动生成答案、题目或未经审核的知识讲解。
- 为了同步而过早加入的强制登录。仅当本地笔记导出/恢复稳定后，才评估可选云同步。

## 9. 版本验收通用标准

1. 内容 ID 全局唯一，父子引用和旧别名有效。
2. 搜索结果能在两次操作内进入目标内容，英语单词/语法可直达卡片。
3. 云图片失败不影响正文，图示的符号、方向、单位和连线正确。
4. 旧收藏、最近浏览、阅读位置、笔记和标签迁移后仍可打开。
5. 开发者工具控制台无项目错误，iPhone 与 Android 不出现溢出、重叠或空白页。
6. 发布前通过内容、搜索、存储迁移、图片、资源清单和发布配置全量检查。

## 10. 推荐的下一步

按风险和收益排序，当前路线固定为：

1. **先闭合 v1.10.1 发布证据**：解决 AppID/开发者工具绑定，生成当前构建包体报告，完成 iPhone 与 Android 实体设备 A1-A15，严格门禁通过后再创建 RC。
2. **再启动 v1.11 内容维护**：数学来源地图和差异报告底座已完成；九年级下册英语已完成官方公开资料再核对，但正式目录和具体标题证据仍不足，不猜测单元、不创建空内容。
3. **持续做五科学科质量维护**：从英语、物理、化学、生物的高风险字段开始，按来源、结构、搜索、图示和文字降级逐批复核，不同时进行大规模改写。
4. **v1.12 以后再评估能力扩展**：只在来源地图稳定、内容回归稳定后，考虑内容生产工具增强和更细的索引；继续保持纯知识查阅定位。

`v1.6` 的公式/单词/语法/实验索引、搜索高亮、笔记筛选、本地备份恢复和阅读显示设置均已完成，不再作为下一步任务重复列入。

当前路线分支的本地质量门禁为 71 项，执行：

```bash
node scripts/check-v1.11-quality-matrix.js
```

需要快速查看当前阻断和下一步时，可执行：

```bash
RELEASE_TOOL_STATE=/path/to/tool-state.json \
CONTENT_SOURCE_FOLLOW_UP=dist/content-audit/content-source-follow-up.json \
node scripts/check-roadmap-status.js
```

该命令只读汇总，不执行上传，也不替代严格发布门禁。

实体设备和当前开发者工具包体证据准备好后，再执行：

```bash
node scripts/check-v1.11-quality-matrix.js --require-release-evidence
```

严格模式还要求开发者工具状态报告为 `ready`，并且当前预览日志没有 `41002` 或其他上传错误；状态报告路径可通过 `RELEASE_TOOL_STATE` 指定。

以下 v1.9.x 和 v1.10.0 小节是历史实施归档，里面的复核队列数量、旧版本包体和“下一步”只用于追溯当时变更，不代表当前状态。当前状态以本节、[v1.11 后续开发路线](./v1.11后续开发路线.md)和发布分支证据目录为准。

一句话路线：先用真实包体和实体设备证据完成 v1.10.1 发布闭环，再用官方资料驱动 v1.11 的五科内容维护，并用元数据源目录阻断未审计的内容漂移，不用猜测扩充目录。

## 11. v1.9 内容审计工具

v1.9 在独立分支 `codex/content-audit-v1.9` 上建立构建期内容审计工具。它统一读取五科静态数据，生成 `dist/content-audit/content-audit.json`，记录实体规模、示例/实验、资源引用、复核覆盖、搜索索引、参考索引和内容差异；发布检查会实际重建并校验报告，报告不进入小程序包体。

v1.9 初始基线报告覆盖 948 个实体、943 条搜索入口和 664 条参考入口；当时 808 个实体已有复核元数据，140 个实体标记为 `untracked`。普通审计会显式报告这些缺口，`--require-reviewed` 会在后续补齐来源与复核记录后作为严格门禁。当前优先补齐数学章节/专题/模板、英语专题/模板和物理模板。详情见 `docs/v1.9内容审计工具实施记录.md`。

后续批次会在每批真实复核后更新当前报告，不把历史统计数字当作当前状态；v1.9.3 第一批完成后当前未跟踪实体为 130 个，数学章节容器剩余 19 个。

v1.9 的审计通过不等于 v1.8 可以发布。实体 iPhone 与 Android 回归、云图片降级和返回栈验证仍是 `1.8.0-rc.1` 的前置门禁。

## 12. v1.9.1 复核队列

从 `codex/content-audit-v1.9` 创建 `codex/content-review-queue-v1.9.1`，将当时 140 个 `untracked` 实体转为确定性复核队列：P1 容器 70 条、P2 方法模板 70 条，数学 94 条、英语 12 条、物理 34 条。队列只提供复核顺序、官方来源候选和证据要求，不改变实体复核状态；当前队列以 v1.9.3 批次记录为准。

复核顺序固定为“容器边界 -> 方法模板 -> 严格门禁”。每批完成后重新生成审计报告和队列，并运行全量内容检查；`check-content-audit.js --require-reviewed` 在所有实体完成真实复核前保持失败。v1.8 实体设备 A1-A10 仍优先于任何 RC 或体验版发布。

## 13. v1.9.2 数学新版目录差异审计

在 `codex/math-curriculum-audit-v1.9.2` 建立数学新版目录审计基线，读取教育部课程标准、国家课程教材目录和人教社新版数学教材介绍，生成 `dist/content-audit/math-curriculum-diff.json`。当前 29 个数学章节继续作为稳定知识容器，不把历史显示顺序误认为新版教材逐册原始章序。

已确认的函数内容拆分已映射到 `ch19-linear-function` 的“函数”和“一次函数”小节；“数据的四分位数”和“数据的分组”记录为待小节映射差异。完整新版册次与章序保持 `needs-official-volume-map`，在逐册官方目录核对完成前不重排章节、不批量修改章号、不删除稳定 ID。

每次发布检查自动运行：

    node scripts/build-math-curriculum-audit.js
    node scripts/check-math-curriculum-audit.js

实施记录见《v1.9.2数学新版目录审计实施记录》。v1.11 已在该审计报告增加数学 `volumeMap` 证据字段、英语九下结构基线和防误填测试，详见《v1.11数学新版目录证据表实施记录》和《v1.11英语九年级下册目录核对实施记录》。该审计不替代生物学 v1.8 的实体设备回归门禁。

## 14. v1.9.3 数学章节容器复核第一批

在 `codex/math-review-v1.9.3` 完成七年级前 10 个数学稳定章节容器的第一批真实复核：有理数、整式的加减、一元一次方程、几何图形初步、相交线与平行线、实数、平面直角坐标系、二元一次方程组、不等式与不等式组、数据的收集整理与描述。

每个容器逐项登记稳定 ID、标题、官方小节、复核日期、两条官方来源和三项证据；repository 只为这 10 章挂载章节级 `contentMeta`。其余 19 个数学章节、29 个数学专题、英语/物理专题和方法模板保持未跟踪状态，复核队列当前为 130 条，其中数学 84、英语 12、物理 34。

本批不改章节顺序、知识点正文、题目、模板、图片、存储版本或实体设备发布门禁。实施记录见《v1.9.3数学章节容器复核实施记录》。

## 15. v1.9.4 数学章节容器复核第二批

在 `codex/math-review-v1.9.4` 完成 `ch11-triangle` 至 `ch29-projection` 共 19 个剩余数学章节容器复核。至此 29 个数学稳定章节全部登记了标题、小节、官方来源、复核范围和证据，数学章节不再进入复核队列。

当前复核队列为 111 条：数学专题 29、数学方法模板 36、英语专题/方法模板 12、物理专题/方法模板 34。下一步进入数学专题容器复核；仍不修改知识正文、题目、图片或新版教材逐册章序。`ch19`、`ch20` 的 `stable-container-only` 边界继续有效。

实施记录见《v1.9.4数学章节容器复核实施记录》。

## 16. v1.9.5 数学专题容器复核

在 `codex/math-topic-review-v1.9.5` 完成 29 个数学专题容器复核。每个专题登记稳定 ID、标题、年级、章节父级映射、核心字段快照哈希、两条官方来源和三项证据；专题页运行时只挂载已复核元数据。

当前复核队列为 82 条：数学方法模板 36、英语专题/方法模板 12、物理专题/方法模板 34。数学章节和专题均不再进入队列。下一步进入数学方法模板复核，随后处理英语和物理方法模板；`ch19/ch20` 的新版目录待核对边界继续有效。

实施记录见《v1.9.5数学专题容器复核实施记录》。

## 17. v1.9.6 数学方法模板复核

在 `codex/math-template-review-v1.9.6` 完成 36 个数学方法模板复核。每个模板登记稳定 ID、标题、分类、章节父级、适用信号、步骤、易错边界、运行时示例数量、图示来源、快照哈希、两条官方来源和三项证据；模板 hydration 只挂载对应的已复核元数据。

复核过程中发现 `model-midpoint` 原有易错边界只有 1 条，已补充“未确认两边中点就直接套用中位线结论”，其余模板方法正文保持不变。共享图示统一记录为 `shared-generated-asset`，13 个模板专属图示记录为 `dedicated-asset`，不把共享资源宣称为独立原创插图。

当前审计覆盖 948 个实体，其中 902 个已复核、46 个待复核；复核队列已不再包含数学章节、专题或方法模板，剩余为英语 12 项和物理 34 项。下一步进入英语/物理容器复核，数学新版逐册章序的 `needs-official-volume-map` 边界继续有效。

实施记录见《v1.9.6数学方法模板复核实施记录》。

## 18. v1.9.7 英语专题容器复核

在 `codex/english-topic-review-v1.9.7` 完成 6 个英语专题容器复核：词汇与构词、句子结构、时态与语态、语法难点、阅读与完形、写作表达。每个专题登记稳定 ID、适用年级、3 个知识点、1 个方法模板、4 个检查点、封面和结构图、快照哈希、两条官方来源和三项证据；repository 和内容审计使用同一份复核元数据。

当前审计覆盖 948 个实体，其中 908 个已复核、40 个待复核；队列剩余物理专题 6 项、英语方法模板 6 项和物理方法模板 28 项。下一步进入物理专题容器复核，英语单元、单词、语法正文保持不变。

实施记录见《v1.9.7英语专题容器复核实施记录》。

## 19. v1.9.8 物理专题容器复核

在 `codex/physics-topic-review-v1.9.8` 完成 6 个物理专题容器复核：运动与声音、光现象与成像、热现象与物质测量、力与流体、功与能量、电学与电磁现象。每个专题登记稳定 ID、适用年级、3 个知识点、1 个方法模板、4 个检查点、封面和独立物理结构图、快照哈希、两条官方来源和三项证据；repository、内容审计和复核检查器使用同一份元数据。

当前审计覆盖 948 个实体，其中 914 个已复核、34 个待复核；队列剩余英语方法模板 6 项、物理方法模板 22 项和物理结构化方法模板 6 项。下一步先处理英语方法模板，再处理物理方法模板；物理章节、知识点、公式、实验和图示正文保持不变。

实施记录见《v1.9.8物理专题容器复核实施记录》。

## 20. v1.9.9 英语方法模板复核

在 `codex/english-template-review-v1.9.9` 完成 6 个英语方法模板复核：词汇四步判断法、句子骨架分析法、时态时间轴判断法、复杂语法定位法、阅读证据定位法、英语写作四段流程。每个模板登记稳定 ID、分类、1 个英语专题父级、4 个适用信号、4 个步骤、易错边界、3 个运行时示例、图示入口、快照哈希、两条官方来源和三项证据；repository、内容审计和复核检查器使用同一份元数据。

当前审计覆盖 948 个实体，其中 920 个已复核、28 个待复核；队列只剩物理章节方法模板 22 项和物理结构化方法模板 6 项。下一步按物理章节模板、物理结构化模板的顺序复核，英语单元、单词、语法、专题和模板正文保持不变。

实施记录见《v1.9.9英语方法模板复核实施记录》。

## 21. v1.10.0 物理方法模板复核

在 `codex/physics-template-review-v1.10.0` 完成 22 个物理章节方法模板和 6 个物理结构化方法模板复核。章节模板覆盖机械运动、声现象、物态变化、光学、密度、力学、热学、电学、电磁学、通信和能源等章节；结构化模板覆盖运动图像、光路图、密度实验、受力分析、能量流向和动态电路。每个模板登记稳定 ID、章节/专题父级、分类、适用信号、方法步骤、易错边界、3 个运行时示例、图示入口、快照哈希、两条官方来源和三项证据；repository、内容审计和复核检查器使用同一份元数据。

当前审计覆盖 948 个实体，其中 948 个已复核、0 个待复核；复核队列为空，物理 158 个实体全部通过严格复核。下一步进入 `--require-reviewed` 严格门禁、实体 iPhone/Android 回归和发布候选准备，保持纯知识查阅范围，不加入答题、测评、错题、登录或云同步。

实施记录见《v1.10.0物理方法模板复核实施记录》。
