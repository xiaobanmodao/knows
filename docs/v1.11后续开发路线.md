# 知识通 v1.11 后续开发路线

> 路线状态：规格已确认，实施从 `codex/roadmap-v1.11` 开始
>
> 当前运行版本：`1.10.1-dev.1`
>
> 发布分支：`codex/release-regression-v1.10.1`

## 1. 目标与边界

v1.11 的目标是把五科学科内容维护变成可重复、可核对、可回归的开发流程，重点解决教材来源映射、内容复核和发布前差异控制。v1.11 不以新增页面或增加学习任务为目标。

产品继续保持纯知识查阅：不加入在线测评、自动评分、学完自测、单元输出任务、错题本、打卡、家长端、强制登录、云同步或未经人工审核的 AI 讲解。

本路线不改变现有稳定 ID、云资源路径、本地存储版本或备案信息。教材照片、自有教辅和公开资料只用于核对课程边界与知识关系，正文、例句、示例、解析和插图继续原创整理。

## 2. 当前规格确认

### 2.1 产品规模

| 学科 | 当前范围 | v1.11 处理方式 |
|---|---|---|
| 数学 | 29 章、29 专题、89 个教材小节、36 个方法模板 | 优先建立新版教材逐册/小节证据映射，不凭历史章序重排 |
| 英语 | 5 册可学习内容、42 个单元、336 个详细词条、84 个语法点；官方结构为 6 册/44 个正式单元 | 维护既有五册；九年级下册已确认 5 个正式单元和 2 个选学戏剧，标题资料核对前保持空缺 |
| 物理 | 22 章、84 个知识点、29 个实验、28 个方法模板 | 继续复核公式、单位、方向、实验和图示，不破坏章节 ID |
| 化学 | 九年级 10 个专题 | 维护专题、知识点、实验、方程式和图片失败降级 |
| 生物 | 6 个单元、36 个知识点、6 个方法模板 | 维护单元、知识点、观察说明和窄屏显示 |

### 2.2 运行与发布规格

- 主包、数学、英语、物理、化学和生物继续按普通分包运行，公共组件和轻量搜索索引留在主包。
- 内容仓库继续使用静态 JavaScript，构建期生成搜索索引、审计报告和资源清单。
- 本地记录继续使用 `subjectId:type:id`，不因路线文档升级存储版本。
- 云图片失败时必须保留完整文字内容；未准备好云环境时不得调用无效云接口。
- 运行时版本在 v1.10.1 发布闭环前保持 `1.10.1-dev.1`；不在路线分支提前伪造 `1.11.0` RC。

### 2.3 本轮确认记录（2026-08-10）

- 继续采用五科学科、游客本地查阅、普通分包和静态内容源的既有规格。
- 本轮不新增测评、任务、错题、打卡、账号或云同步能力；页面运行层增加纯知识文案门禁，正文中的正常学科语境不作为任务功能处理。
- 已将可见入口中的“模板题、集中练、题型模型”等练习化表述统一为“方法模板、查阅例题”；原创例题仍作为知识讲解材料保留，不新增答题或评分流程。
- 数学来源地图和差异报告工具已补齐：报告同时核对 29 个稳定章节、89 个稳定 `lessonId`、89 条旧别名，输出 10 项逐册证据字段、待映射章节清单及新增/修改/删除汇总；当前仍保持 `needs-official-volume-map`。
- 已新增元数据级结构化内容源目录：覆盖 948 个实体和 89 条数学旧别名，保存稳定键、父级、内容哈希、复核状态、来源键及资源数量，不复制正文、例句、公式或图片路径；构建产物与当前源不一致时会在发布准备阶段失败。
- 已完成全学科内容批次首轮干跑：批次清单覆盖当前内容源全部 948 个实体、23 个学科/类型范围，逐批校验实体数量、示例/实验/资源统计、类型隔离和零差异；覆盖门禁会阻断未登记或过期范围，旧别名仍由内容源目录单独校验。
- 发布分支仍等待有效 AppID 上传链路、当前构建包体报告和 iPhone/Android 实体设备证据；路线分支继续推进来源地图与质量工具，不创建 RC。

## 3. 阶段 0：完成 v1.10.1 发布闭环

阶段 0 是 v1.11 继续开发前的发布前置，但发布分支和路线分支必须隔离。

### 3.1 外部状态确认

在微信开发者工具中完成以下操作：

1. 确认当前登录账号拥有 `project.config.json` 中 AppID 的开发权限。
2. 关闭并重新打开当前项目，确认项目绑定的 AppID 与配置一致。
3. 运行预览/上传前置流程，保留完整日志，不把“Using AppID”作为成功证据。
4. 如仍出现 `41002 appid missing`，先处理账号、项目绑定或开发者工具状态，不修改业务代码绕过服务端校验。

发布诊断可在不改动项目的情况下运行：

```bash
cd /Users/hht/Desktop/knows/.worktrees/roadmap-v1.11
node scripts/check-release-tool-state.js --project /Users/hht/Desktop/knows/.worktrees/release-regression-v1.10.1
```

该命令读取项目配置、开发者工具 `islogin` 输出和最近一次 `preview.log`，写入 `tool-state.json`。只有同时出现有效 AppID、已登录和无上传错误时才显示 `ready`；`41002` 会明确记录为 `appid-permission-or-project-binding`，不能替代包体报告或实体设备证据。

### 3.2 当前构建证据

在发布工作树 `/Users/hht/Desktop/knows/.worktrees/release-regression-v1.10.1` 执行：

```bash
node scripts/run-release-preview.js
```

必须生成并核对：

- `.codex-output/release-regression-v1.10.1/preview.log`
- `.codex-output/release-regression-v1.10.1/preview-qr.png`
- `.codex-output/release-regression-v1.10.1/packages-preview.json`

包体报告必须来自当前最终构建，不能使用历史报告、空 JSON 或手工填写的数字。目标是主包小于 `700 KiB`，每个学科分包小于 `1 MB`。

### 3.3 实体设备门禁

使用 `docs/v1.10发布前实体设备回归清单.md` 和 `docs/release-regression/v1.10.0-evidence.template.json`，在 iPhone 与 Android 实体设备各执行 A1-A15。每台设备至少记录型号、系统版本、微信版本、网络状态、构建引用、测试时间、截图和控制台错误数量。

严格检查命令：

```bash
RELEASE_REGRESSION_EVIDENCE=.codex-output/release-regression-v1.10.1/evidence.json \
node scripts/check-release-readiness.js --require-device-evidence
```

只有设备证据、包体报告、五科内容检查、运行时依赖检查和发布配置检查全部通过后，才允许在发布分支创建 `1.10.1-rc.1` 并上传体验版。

## 4. 阶段 1：v1.11 来源地图与内容维护

### 4.1 数学新版目录映射

数学新版教材的处理顺序固定为：官方目录证据收集 -> 结构差异报告 -> 稳定 ID 映射 -> 页面显示核对 -> 回归检查。

已公开确认的结构变化可以记录为映射；未取得逐册官方目录时，必须保留 `needs-official-volume-map` 边界。不得因为目录顺序变化而：

- 直接用章节序号重建主键；
- 批量重命名 `ch*`、专题、知识点或模板稳定 ID；
- 删除现有章节或将历史内容强行归入猜测的新版章节；
- 用搜索结果、培训机构目录或单张教材照片替代官方逐册证据。

每个完成的映射至少记录：稳定 ID、教材版本、册次、显示标题、官方小节、来源 URL/PDF、核对日期、变更原因和是否影响旧别名。

### 4.2 英语教材映射

英语五册现有单元和详细词法/语法内容继续保持可查阅。人教社公开介绍已确认九年级下册有 5 个正式学习单元和 2 个选学戏剧；只有在正式目录、单元标题和可靠教材文件完成交叉核对后才扩充。此前只记录结构待核对项，不创建空单元、不编造单元标题、不把能力专题伪装成教材单元。

已有词条和语法点的维护必须保留英式主拼写、美式变体、IPA、词性、可数性/及物性、搭配、原创例句、辨析和复核来源字段。

### 4.3 物理、化学和生物维护

按小批次复核以下高风险字段：

- 物理：符号、单位、方向、公式条件、实验步骤、误差、安全提醒和图示连线；
- 化学：化学式、反应条件、配平、实验现象、结论和图片文字降级；
- 生物：术语、观察对象、实验步骤、结论、单元父级和窄屏文字布局。

每次只处理一个明确批次，完成后立即生成差异报告和检查结果。内容准确性问题与页面交互问题分开提交，避免难以定位回归来源。

## 5. 阶段 2：内容生产与审计底座

在阶段 1 的来源地图稳定后，再逐步建设：

1. 结构化内容输入格式，支持稳定 ID、父级、别名、来源和复核状态；
2. 构建期 schema 校验、搜索索引生成、图片资源检查和差异报告；
3. 人工复核队列，区分待核对、已核对和已发布状态；
4. 小批次内容预览，确保正文、图示和搜索结果同时可回归。

当前已先完成元数据目录阶段：`scripts/build-content-source-catalog.js` 生成 `dist/content-audit/content-source-catalog.json`，`scripts/check-content-source-catalog.js` 校验源哈希、父级引用、旧别名和禁止正文字段，并支持通过 `CONTENT_SOURCE_CATALOG_BASELINE` 输出新增/修改/删除差异、通过 `--require-no-diff` 作为阻断门禁。输入协议由 `scripts/content-source-input.js` 负责 JSON/CSV 行归一化，`scripts/build-content-source-input.js` 生成带 `inputHash` 的规范化 JSON，支持 `--from-current --batch <batchId>` 按登记批次导出；`scripts/check-content-source-input.js --batch <batchId> --report <report.json>` 可对外部 JSON/CSV 输入校验范围、统计和差异，并留存输入哈希、当前源哈希、复核统计和变更明细。`scripts/check-content-source-batches.js` 固化全部 23 个学科/类型批次的实体数量、类型隔离、示例/实验/资源统计、复核状态和零差异要求，并可通过 `--report dist/content-audit/content-source-batches.json` 生成确定性批次报告。新增 `scripts/check-subject-high-risk-batches.js` 登记英语 5 册和物理 3 册共 8 个高风险批次，逐批校验词法/语法补深、物理公式契约、单位、方向、实验和复核状态，并生成 `dist/content-audit/subject-high-risk-batches.json`；新增 `scripts/check-chemistry-biology-high-risk-batches.js` 按化学 5 个主题批次和生物 6 个专题批次登记高风险字段，逐批校验化学方程式/实验、生物观察/例子和复核状态，并生成 `dist/content-audit/chemistry-biology-high-risk-batches.json`。当前批次审计覆盖 948 个实体、1,987 个示例、49 个实验和 1,885 个资源，复核状态为 900 个 verified、48 个 reviewed；旧别名仍由内容源目录校验。这些产物只服务构建审计，不改变运行时数据来源。

运行时仍加载构建产物，不把数据库、后台编辑器、登录或云同步引入 v1.11 的第一批任务。

## 6. 分支与提交规则

### 发布分支

`codex/release-regression-v1.10.1` 只接受以下变更：发布配置、包体/设备证据工具、云运行时阻塞修复、与发布直接相关的文档。未通过严格门禁前，不创建 RC。

### 路线分支

`codex/roadmap-v1.11` 用于路线文档、来源地图工具和后续内容维护。路线分支不代替发布分支的实体设备证据，也不把未发布内容上传为正式体验版。

### 热修复分支

如果 v1.10.1 实体回归发现问题，从最终冻结提交建立独立 `codex/release-hotfix-*` 分支。热修复只包含复现所需的最小改动，不合并 v1.11 内容。

### 单批提交顺序

```text
规格/来源记录 -> 数据或工具实现 -> 页面适配 -> 校验与差异报告 -> 文档 -> 回归结果
```

每个提交保持一个可解释主题；不要把大批量内容改写、图片替换和路由重构放进同一提交。

## 7. 统一验收门槛

每一批 v1.11 变更至少通过：

```bash
node scripts/check-release-readiness.js
node scripts/check-content-review-meta.js
node scripts/check-content-audit.js --require-reviewed
node scripts/check-subject-content.js
node scripts/check-content-schema.js
node scripts/check-content-diff.js
node scripts/check-search-index.js
node scripts/check-search-semantics.js
node scripts/check-content-routes.js
node scripts/check-package-manifest.js
node scripts/check-package-boundaries.js
node scripts/check-pure-knowledge-runtime.test.js
node scripts/check-pure-knowledge-runtime.js
node scripts/check-student-copy.js
node scripts/check-content-migration.js
node scripts/check-note-filters.js
node scripts/check-local-backup.js
node scripts/check-reading-display.js
node scripts/check-cloud-assets-runtime.js
node scripts/check-unique-figures.js
git diff --check
```

以上检查也可由 `node scripts/check-v1.11-quality-matrix.js` 按固定顺序统一执行，当前共 60 项，包含五科全局内容契约、内容源输入、外部输入审计报告、内容差异报告、英语/物理及化学/生物高风险批次、JSON/CSV 构建、批次审计与报告、开发者工具状态诊断、目录契约和构建产物检查。实体设备和当前构建包体证据准备好后，再执行 `node scripts/check-v1.11-quality-matrix.js --require-release-evidence`；严格模式会要求 iPhone/Android 证据和当前 `packages-preview.json`，当前开发阶段不默认执行。

涉及学科时追加对应准确性检查：

```bash
node scripts/check-math-accuracy.js
node scripts/check-english-accuracy.js
node scripts/check-physics-accuracy.js
```

涉及化学或生物时，必须同时运行对应的专题/目录/资源检查脚本，并在提交说明中记录本批没有覆盖的范围。

## 8. 暂不做的事情

- 不在 AppID、包体和实体设备证据缺失时创建 RC；
- 不把模拟器截图写成实体设备通过；
- 不猜测新版数学逐册章序或英语九下单元；
- 不复制教材正文、课文、题目、解析、听力材料或原书插图；
- 不引入测评、任务、错题、打卡、家长端、登录、云同步或自动生成内容；
- 不升级本地存储版本，不改稳定 ID，不删除历史别名；
- 不因一次内容批次顺便重构公共路由、分包边界或云函数。

## 9. 下一批具体任务

实施顺序已确认如下：

1. 完成发布分支的 AppID/开发者工具绑定，生成真实包体报告；
2. 由用户在 iPhone 和 Android 实体设备完成 A1-A15 并保存证据文件；
3. 发布分支通过严格门禁后创建 `1.10.1-rc.1`，上传体验版并单独处理审核反馈；
4. [已完成] 在本路线分支新增数学新版逐册目录证据表和差异报告字段；
5. 核对英语九年级下册官方资料，确认后再决定是否建立单元数据；
6. [已完成] 建立元数据级结构化内容源目录，并将源哈希与构建产物一致性接入发布准备；
7. [已完成] 增加内容源目录基线差异审计，支持新增、修改、删除分类和显式零差异门禁；
8. [已完成] 建立元数据 JSON/CSV 输入协议，校验稳定 ID、父级、别名、来源、复核状态和内容哈希，并支持按学科/实体类型导出明确批次；
9. [进行中] 完成真实内容小批导入审计，英语 `unit=42`、`word=336`、`grammar=84` 和全学科 23 批次已完成当前源零差异报告；物理 `knowledge=84` 已完成带输入哈希的报告样例，外部 JSON/CSV 首批输入仍需接入；物理高风险字段专项检查已通过，继续按批次保留复核证据；
10. 按数学、英语、物理、化学、生物的高风险字段顺序做小批复核；
11. 每批通过结构、准确性、搜索、资源和回归检查后再推送。

第 4、6、7、8 项已完成；第 9 项已完成全学科 23 个批次、948 个实体的首轮零差异干跑，物理公式、单位、方向和实验字段继续由专项准确性检查覆盖，再进入内容维护。发布体验版相关工作仍等待 AppID 绑定、包体报告和实体设备证据。

数学新版目录证据表当前由 `packages/math/data/math-curriculum-baseline.js` 保存，差异报告由 `scripts/build-math-curriculum-audit.js` 生成；报告中的 `sourceMap`、`stability` 和 `diffSummary` 分别负责证据覆盖、稳定 ID/旧别名和结构变化汇总；`scripts/math-volume-map.test.js` 会拒绝把未核对的官方册次、章号或标题写入报告。

物理、化学和生物本批高风险字段复核结果见《v1.11五科学科高风险字段复核记录》。
