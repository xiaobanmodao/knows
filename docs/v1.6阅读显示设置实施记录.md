# v1.6 阅读显示设置实施记录

当前版本：`1.6.0-dev.1`

开发分支：`codex/reference-indexes-v1.6`

验证日期：2026-08-01

## 1. 实施范围

本阶段为数学、英语和物理知识页增加统一的 `Aa 阅读` 设置，并在“我的”页提供同一入口。可选项固定为：

- 字号：小、标准、大。
- 行距：紧凑、标准、舒展。
- 图片宽度：窄 `72%`、适中 `86%`、全宽 `100%`。

默认为标准字号、标准行距和全宽图片。阅读令牌只作用于正文、讲解、实验内容、题目内容、笔记正文和教学图片；标题、标签、按钮、固定字段标签和导航保持原尺寸。知识页通过 `onShow` 重新读取偏好，以接收从“我的”页返回后的变更。

## 2. 实施文件

- 偏好和应用接口：`utils/reading-preferences.js`、`utils/storage.js`、`app.js`。
- 备份兼容：`utils/local-backup.js`、`scripts/check-local-backup.js`。
- 共享设置组件：`components/reading-settings/index.js`、`index.json`、`index.wxml`、`index.wxss`。
- 共享正文与样式：`components/content-block/index.js`、`index.wxml`、`index.wxss`、`styles/reading-display.wxss`。
- 三科知识页：`packages/math/pages/knowledge/index.*`、`packages/english/pages/knowledge/index.*`、`packages/physics/pages/knowledge/index.*`。
- “我的”页：`pages/profile/index.js`、`index.json`、`index.wxml`、`index.wxss`。
- 聚焦契约检查：`scripts/check-reading-display.js`。

## 3. 偏好与备份契约

阅读偏好使用独立本地键，完整结构为：

```js
{
  version: 1,
  fontSize: 'standard',
  lineHeight: 'standard',
  imageWidth: 'full'
}
```

- 缺失、未知或非法字段按字段回退到默认值，公共方法始终返回完整的归一化对象。
- 保存失败时页面保留本次会话偏好并提示“设置未保存”，不改写阅读位置、收藏、笔记或折叠状态。
- 内容存储版本保持 `4`，备份文件格式保持 `knows-local-backup` v1。
- 新备份包含归一化后的 `readingPreferences`。恢复新备份时应用其偏好；旧备份缺少该字段时保留当前偏好。
- 阅读偏好参与恢复的原子写入和回滚，任一后续写入失败时恢复到导入前快照。

## 4. 自动检查证据

2026-08-01 在仓库根目录中按以下顺序逐条独立运行，每条均以退出码 `0` 结束：

| 命令 | 实际结论 |
|---|---|
| `node scripts/check-reading-display.js` | `OK reading preference normalization, classes and labels checked` |
| `node scripts/check-note-filters.js` | `OK 4 note records, 3 subject facets, 6 tag facets and 7 filter combinations checked` |
| `node scripts/check-local-backup.js` | `OK backup v1, file limits, checksum, merge, replace and rollback checked` |
| `node scripts/check-english-units.js` | `OK 5 books, 42 units, 336 words, 84 grammar points, 924 unique examples, 468 unique IDs checked` |
| `node scripts/check-english-accuracy.js` | `OK 5 verified books, 42 units, 336 words, 84 grammar points checked for language accuracy boundaries` |
| `node scripts/check-english-depth.js` | `OK 42 units, 336 detailed words, 84 detailed grammar points and 924 examples checked` |
| `node scripts/check-physics-curriculum.js` | `OK 3 books, 22 chapters, 84 knowledge points, 22 templates, 252 examples, 29 experiments, 383 unique IDs checked` |
| `node scripts/check-physics-accuracy.js` | `OK 22 chapters science boundaries and 29 experiment safety records checked` |
| `node scripts/check-physics-depth.js` | `OK 84 physics details, 225 quantity links, 32 direction rules and 29 structured experiments checked` |
| `node scripts/check-subject-content.js` | `OK 3 subjects, 1202 globally unique content IDs checked` |
| `node scripts/check-math-content.js` | `OK 29 chapters checked` |
| `node scripts/check-math-accuracy.js` | `OK 29 chapters, 89 lessons, 445 examples, 36 legacy entries checked for mathematical accuracy boundaries` |
| `node scripts/check-math-depth.js` | `OK 89 math details, 89 unique derivations, 89 unique reasons and 12 cross-subject links checked` |
| `node scripts/check-unique-figures.js` | `OK 716 unique knowledge/problem figures checked` |
| `node scripts/check-content-migration.js` | `OK content storage migration checked` |
| `node scripts/check-content-review-meta.js` | `OK review metadata checked for 89 math lessons, 42 English units, 336 words, 84 grammar points and 84 physics points` |
| `node scripts/check-search-experience.js` | `OK 39 search rankings, English/physics/math anchors and subject filtering checked` |
| `node scripts/check-search-index.js` | `OK 833 generated search entries and source hash checked` |
| `node scripts/check-search-semantics.js` | `OK 22 alias groups, 5 normalizations, highlight ranges and formula lookups checked` |
| `node scripts/check-reference-index.js` | `OK 622 reference entries: 89 math formulas, 84 physics formulas, 336 words, 84 grammar points and 29 experiments checked` |
| `node scripts/check-content-routes.js` | `OK 8 routes and 8 legacy path shims checked` |
| `node scripts/check-package-boundaries.js` | `OK main package isolation, 3 package boundaries and no preload rule checked` |
| `node scripts/check-content-schema.js` | `OK 836 schema entities, 209 formulas, 89 reasoning blocks, 35 experiments and parent references checked` |
| `node scripts/check-content-diff.js` | `OK content diff: +0 ~710 -0 -> dist/content-audit/content-diff.json` |
| `node scripts/check-cloud-assets-runtime.js` | `OK cloud assets use server signing first, client fallback, 50-item batches and text-safe failure` |
| `node scripts/check-release-readiness.js` | `OK release readiness checked` |

## 5. Developer Tools 与模拟器证据

已检测到本机安装并正在运行微信开发者工具 `2.02.2607171`。尝试执行：

```bash
/Applications/wechatwebdevtools.app/Contents/MacOS/cli open --project /Users/hht/Desktop/knows --lang zh
```

命令以退出码 `246` 结束，工具明确返回 `IDE service port disabled`。随后通过已运行的开发者工具进程尝试 UI 自动化取得可访问性树和截图，持续等待约一分钟仍无任何返回，已终止挂起调用。

因此，本次没有可信证据证明项目已在 iPhone 14 Pro Max 或 Nexus 5 模拟器上完成编译和交互验收。下列项目均为未验证，不标记通过：

- 三科代表知识点、三组控件的全选项交互及默认布局对照。
- 中文段落、IPA、长单词、公式、单位、表格和实验行的溢出检查。
- `72%`、`86%`、`100%` 图片的居中、无裁切和云图失败稳定性。
- “我的”页变更后的 `onShow` 同步、恢复默认后重启持久化。
- 新备份恢复阅读偏好、旧备份保留当前偏好。
- 收藏、笔记、折叠、题目展开、上下篇导航和继续阅读位置回归。
- Developer Tools 控制台零项目错误和页面无不连贯重叠。

## 6. 包体证据

因 IDE 服务端口关闭，本次无法生成新的 Developer Tools 预览包报告。`.codex-output/v1.4-packages-preview.json` 的本地修改时间为 2026-08-01 15:49:46，早于阅读显示设置 19:40 至 20:17 的功能提交，因此未将其当作当前包体证据，也未使用该旧文件声称 `check-package-sizes.js` 通过。

可获得的保守证据如下：

- `node scripts/check-package-boundaries.js` 退出码为 `0`，主包未直接引用三科全文，三个分包之间无交叉引用，且未配置预下载。
- 按 `project.config.json` 的打包忽略边界，对 `HEAD` 跟踪的运行时源文件求和：主包约 `849.9 KiB`、英语约 `569.4 KiB`、数学约 `600.9 KiB`、物理约 `277.5 KiB`。
- 上述数字是未经 Developer Tools 压缩、编译和包装的原始源码估算，不是预览包精确大小。三科原始源文件低于各 `1 MiB` 目标；主包原始源文件高于 `700 KiB`，所以现有证据不能证明当前主包精确低于目标。

## 7. 实体机与剩余验收

本次未连接实体 iPhone 或 Android 设备，因此实体机上的三科排版、图片降级、本地持久化、备份恢复和原有阅读工作流均保留为发布前手工验收项。还需在服务端口可用时重新生成当前预览包报告，再运行 `node scripts/check-package-sizes.js` 确认主包与三科分包的精确门槛。
